iniclear launcher

This clears auto boot then boot the menu.

ttmenu.dat: ttio dldi-patched. Use it as DSTT kernel. Now I can safely use ysloader...
akmenu4.nds: rpgs dldi-patched. Use it as AK2i kernel.
